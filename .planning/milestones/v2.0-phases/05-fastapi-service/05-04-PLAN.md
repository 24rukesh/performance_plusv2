---
phase: 05-fastapi-service
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - tests/test_api.py
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-05]

must_haves:
  truths:
    - "tests/test_api.py covers every endpoint with an auth-required test and a happy-path test"
    - "GET /api/health test asserts 200 + exact response dict (no auth required)"
    - "POST /api/analyze test asserts 401 without X-API-Key, 422 on empty input, 422 on duplicate session_id, 422 on no session_id overlap, and 200 + AnalysisResult JSON on valid input (DEMO_MODE=1 fixture-backed)"
    - "POST /api/analyze test asserts the database persistence side effect: insert_analysis_result called once per campaign with a shared UUID run_id"
    - "POST /api/webhook/crm test asserts 401 without X-API-Key, 202 + insert_pending_session called with the parsed CrmWebhookRecord on valid request"
    - "GET /api/campaigns/{id}/actions test asserts 401 without X-API-Key, 404 when get_latest_result returns None, and 200 + the JSONB dict when get_latest_result returns a dict"
    - "All tests pass with `uv run pytest tests/test_api.py -v` returning exit code 0"
    - "Tests run without a live Postgres or live OpenAI API — db functions are monkeypatched, llm fixture is loaded via DEMO_MODE=1"
  artifacts:
    - path: "tests/test_api.py"
      provides: "FastAPI TestClient-based contract tests for all 5 endpoints + auth gates + persistence side effects"
      min_lines: 100
      contains: "from fastapi.testclient import TestClient"
  key_links:
    - from: "tests/test_api.py"
      to: "api.main.app"
      via: "TestClient(app)"
      pattern: "TestClient\\(app\\)"
    - from: "tests/test_api.py"
      to: "api.db monkeypatching"
      via: "monkeypatch.setattr('api.db.insert_analysis_result', ...)"
      pattern: "monkeypatch\\.setattr.*api\\.db"
    - from: "tests/test_api.py"
      to: "llm DEMO_MODE fixture"
      via: "monkeypatch.setenv('DEMO_MODE', '1') + monkeypatch.setattr('llm.client', None)"
      pattern: "DEMO_MODE.*1"
---

<objective>
Add `tests/test_api.py` — pytest + FastAPI TestClient contract tests that exercise all 5 endpoints, the X-API-Key auth gate behavior (401 vs 200/202/422/404), and the database persistence side effects (insert_analysis_result loop with shared UUID run_id, insert_pending_session call, get_latest_result return-value handling). Tests use the existing DEMO_MODE=1 + fixture path in llm.py so no live gpt-4o call is required, and monkeypatch the api.db functions so no live Postgres is required.

Purpose: Provide the automated verification that closes ALL 5 phase requirements (API-01 through API-05) as behavior, not just shape. After this plan, `uv run pytest tests/ -v` produces a clean test report demonstrating Phase 5 is functionally complete and ready for `/gsd:verify-phase`.

Output: A single tests/test_api.py file (~150-250 lines, 10-14 test functions). After this plan: `uv run pytest tests/test_api.py -v` exits 0; existing tests (test_data, test_llm, test_ui_helpers, test_deploy_config) remain unaffected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fastapi-service/05-CONTEXT.md
@.planning/phases/05-fastapi-service/05-RESEARCH.md
@.planning/phases/05-fastapi-service/05-PATTERNS.md
@.planning/phases/05-fastapi-service/05-AI-SPEC.md
@./tests/test_llm.py
@./llm.py
@.planning/phases/05-fastapi-service/05-01-PLAN.md
@.planning/phases/05-fastapi-service/05-03-PLAN.md

<interfaces>
<!-- Contracts already defined in earlier plans that test_api.py consumes. -->

From api.main (Plan 03):
- `app: FastAPI` — module attribute; pass to TestClient
- Endpoints under test: GET /api/health, POST /api/analyze, POST /api/webhook/crm, GET /api/campaigns/{campaign_id}/actions
- Auth dependency: verify_api_key reads X-API-Key header and compares against os.environ["API_KEY"]; returns 401 on mismatch
- Lifespan: calls init_db() at startup — tests MUST monkeypatch init_db to no-op before TestClient context manager invokes lifespan (otherwise tests need a real DB)

From api.models (Plan 01):
- AnalyzeRequest fields: web_sessions (list[WebSession]), crm_records (list[CrmRecord])
- WebSession fields: session_id, campaign_id, clicks, impressions, cost_usd, conversion_rate
- CrmRecord fields: session_id, campaign_id, lead_status, projected_value, sales_notes
- CrmWebhookRecord fields: same 5 as CrmRecord

From api.db (Plan 01) — functions to monkeypatch in tests:
- init_db() — patch to lambda: None (no real DB)
- insert_analysis_result(campaign_id, run_id, result) — patch with MagicMock to inspect call args
- get_latest_result(campaign_id) — patch with MagicMock to control return value
- insert_pending_session(record) — patch with MagicMock to inspect call args

From llm (existing — Phase 3):
- DEMO_MODE pattern: when os.environ["DEMO_MODE"]=="1" AND llm.client is None, run_analysis returns the fixture from data/fixture_results.json instead of calling gpt-4o
- The fixture exists (Phase 3 shipped) and parses to a valid AnalysisResult with `campaigns` non-empty
- Test fixture file is at: data/fixture_results.json (loaded via Path(__file__).parent / "data" / "fixture_results.json" in llm._load_fixture)

From tests/test_llm.py (existing — pattern reference):
- Pattern: `monkeypatch.setenv("DEMO_MODE", "1")` + `monkeypatch.setattr("llm.client", None)` to force the fixture branch
- Pattern: `from unittest.mock import MagicMock, patch` + `@patch("llm.client")` for fine-grained control

From RESEARCH.md Code Examples section "httpx TestClient pattern":
- `from fastapi.testclient import TestClient` (not `from starlette.testclient` — FastAPI re-exports it)
- `client = TestClient(app)` — synchronous test client
- Use TestClient as a context manager (`with TestClient(app) as client:`) when you need lifespan startup events to fire — required here because the analyze endpoint depends on init_db being called (we monkeypatch init_db before entering the context).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_api.py with TestClient-based contract tests covering all 5 endpoints, auth gates, and DB side effects</name>
  <files>tests/test_api.py</files>
  <read_first>
    - ./tests/test_llm.py (full file — copy the DEMO_MODE monkeypatch idiom from lines 139-146; copy the MagicMock + patch decorator idiom from lines 47-53; copy the conftest-free pattern — tests in this repo do not use conftest.py)
    - ./llm.py (verify _load_fixture() loads from data/fixture_results.json; verify run_analysis() returns AnalysisResult; understand DEMO_MODE branch at lines 99-100)
    - ./data/fixture_results.json (must exist from Phase 3 — confirm by `ls data/fixture_results.json`; the test uses this implicitly via run_analysis when DEMO_MODE=1 and llm.client is None)
    - .planning/phases/05-fastapi-service/05-RESEARCH.md (Code Examples "httpx TestClient pattern" lines 690-717; Eval Strategy Section 5 — full eval rubric to mirror in test coverage; Common Pitfall 4 — sys.path / import; the project root is on sys.path when running via `uv run pytest` from the project root)
    - .planning/phases/05-fastapi-service/05-PATTERNS.md (Pattern S6 "Test Fixture Pattern" lines 654-685 — TestClient + DEMO_MODE pattern verbatim)
    - .planning/phases/05-fastapi-service/05-AI-SPEC.md (Section 5 Evaluation Strategy — eval dimensions that translate to test assertions; Section 6 Guardrails — every online guardrail row should have at least one test)
    - .planning/phases/05-fastapi-service/05-CONTEXT.md (D-12 auth gate, D-03 response shape, D-06 webhook 202, D-07 webhook stores not analyzes, D-10 one row per campaign, D-11 webhook record schema)
    - api/main.py (created in Plan 03 — the file under test; specifically verify the route paths, the verify_api_key dependency signature, and the lifespan reference to init_db so the monkeypatch target is correct)
    - api/models.py (created in Plan 01 — for constructing valid request payloads)
    - api/db.py (created in Plan 01 — for monkeypatch targets: api.db.init_db, api.db.insert_analysis_result, api.db.get_latest_result, api.db.insert_pending_session)
  </read_first>
  <action>
    Create `tests/test_api.py` containing the test functions listed below. Module-level imports: `import os`, `from unittest.mock import MagicMock`, `import pytest`, `from fastapi.testclient import TestClient`. Do NOT import `api.main.app` at module level — import it INSIDE a `_make_client(monkeypatch)` helper AFTER applying env var + monkeypatch.setattr so the auth gate and lifespan see the patched state. The helper monkeypatches `api.db.init_db` to a no-op lambda, sets `os.environ["API_KEY"] = "test-key"` via `monkeypatch.setenv`, sets `DEMO_MODE=1` via `monkeypatch.setenv`, sets `llm.client = None` via `monkeypatch.setattr`, then `from api.main import app` and `return TestClient(app)` wrapped as a context manager (use `monkeypatch.setattr` only — pytest's `monkeypatch` fixture auto-rolls-back at test end, which keeps tests isolated).

    Required test functions (each must pass independently):

    (1) `test_health_returns_status_version_service(monkeypatch)` — no auth header required. GET /api/health. Assert status_code == 200 and `response.json() == {"status": "ok", "version": "2.0.0", "service": "performance-plus-api"}` (API-05).

    (2) `test_health_no_auth_required(monkeypatch)` — set `API_KEY=test-key` env var; GET /api/health WITHOUT X-API-Key header. Assert status_code == 200 (negative case for D-12 — /api/health is the only endpoint without auth).

    (3) `test_analyze_missing_api_key_returns_401(monkeypatch)` — POST /api/analyze with a valid body but NO X-API-Key header. Assert status_code == 401 and `"Invalid or missing API key"` in response detail (API-01 auth gate).

    (4) `test_analyze_wrong_api_key_returns_401(monkeypatch)` — POST /api/analyze with X-API-Key set to a value DIFFERENT from the API_KEY env var. Assert status_code == 401 (API-01 auth gate).

    (5) `test_analyze_empty_arrays_returns_422(monkeypatch)` — POST /api/analyze with valid auth header but `{"web_sessions": [], "crm_records": []}`. Assert status_code == 422 and the message mentions "non-empty" (Pitfall AI-SPEC empty input guard).

    (6) `test_analyze_duplicate_crm_session_id_returns_422(monkeypatch)` — POST /api/analyze with valid auth and a payload where `crm_records` contains two entries with the same `session_id`. Assert status_code == 422 and the message contains "Duplicate session_id" (Pitfall 5 — validate="m:1" guard).

    (7) `test_analyze_no_session_overlap_returns_422(monkeypatch)` — POST /api/analyze with web_sessions and crm_records that share NO session_id values (e.g., web has session_id="w1", crm has session_id="c1"). Assert status_code == 422 and message contains "No session_id overlap" (Pitfall 7 — empty merge guard).

    (8) `test_analyze_happy_path_returns_200_with_analysis_result(monkeypatch)` — Set DEMO_MODE=1 and llm.client=None (forces fixture branch). Monkeypatch `api.db.insert_analysis_result` to a MagicMock. POST /api/analyze with valid auth and a payload that merges to at least 1 row (web session and crm record with the same session_id + campaign_id). Assert status_code == 200, response JSON contains `executive_summary` (str) and `campaigns` (non-empty list), each campaign has `campaign_id`, `budget_action`, `percentage_change`, `semantic_reasoning`, `confidence`, `evidence_count` (API-01).

    (9) `test_analyze_persists_one_row_per_campaign_with_shared_run_id(monkeypatch)` — Same setup as test 8. Monkeypatch insert_analysis_result with a MagicMock. After successful POST, inspect mock.call_args_list: assert `mock.call_count == len(response_campaigns)` (one call per campaign); assert all calls share the same `run_id` argument (second positional arg) and that this run_id is a valid UUID string (`uuid.UUID(call.args[1])` does not raise); assert the set of `campaign_id` first-positional args equals the set of `campaign_id` values in the response (API-02 persistence with shared run_id per Open Question 3 in RESEARCH).

    (10) `test_webhook_missing_api_key_returns_401(monkeypatch)` — POST /api/webhook/crm with valid body but no X-API-Key. Assert status_code == 401 (API-03 auth gate).

    (11) `test_webhook_happy_path_returns_202_and_calls_insert_pending(monkeypatch)` — Monkeypatch `api.db.insert_pending_session` with a MagicMock. POST /api/webhook/crm with valid auth and a valid CrmWebhookRecord body. Assert status_code == 202 and `response.json() == {"status": "accepted"}`. Assert insert_pending_session was called exactly once. Assert the call argument has the expected `campaign_id`, `session_id`, `lead_status`, `projected_value`, `sales_notes` attribute values (CrmWebhookRecord parsed correctly) (API-03 happy path + persistence).

    (12) `test_campaigns_actions_missing_api_key_returns_401(monkeypatch)` — GET /api/campaigns/cmp_test/actions with no X-API-Key. Assert status_code == 401 (API-04 auth gate).

    (13) `test_campaigns_actions_returns_404_when_no_results(monkeypatch)` — Monkeypatch `api.db.get_latest_result` to a MagicMock returning None. GET /api/campaigns/nonexistent/actions with valid auth. Assert status_code == 404 (API-04 not-found behavior).

    (14) `test_campaigns_actions_returns_latest_result_dict(monkeypatch)` — Monkeypatch `api.db.get_latest_result` to a MagicMock returning a sample dict like `{"executive_summary": "x", "campaigns": [{"campaign_id": "cmp_test", "budget_action": "increase", "percentage_change": 25, "semantic_reasoning": "good signal", "confidence": 0.8, "evidence_count": 3}]}`. GET /api/campaigns/cmp_test/actions with valid auth. Assert status_code == 200 and the response JSON equals the mocked dict exactly. Assert get_latest_result was called with `"cmp_test"` (the path parameter) (API-04 happy path).

    For all DB-side-effect tests, the `monkeypatch.setattr` MUST target `api.db.<function>` BEFORE importing `api.main` — but more importantly, it must also target `api.main.<function>` (the imported name in api/main.py's namespace) OR alternatively the helper imports api.main first then patches `api.main.insert_analysis_result` directly. Choose the api.main.<name> patching path since api/main.py uses `from api.db import insert_analysis_result, ...` — that creates a new local binding that is independent of further changes to api.db.<name>. The helper signature: `def _make_client(monkeypatch, db_overrides: dict | None = None)` — for each (name, mock) in db_overrides, do `monkeypatch.setattr(f"api.main.{name}", mock)` after importing api.main and before entering the TestClient context.

    Use `with TestClient(app) as client:` (context-manager form) so the lifespan fires — but because init_db is monkeypatched to a no-op (in api.main namespace too, via `monkeypatch.setattr("api.main.init_db", lambda: None)`), the lifespan does not need a real DB connection.

    Do NOT depend on a live OPENAI_API_KEY — DEMO_MODE=1 + llm.client=None forces the fixture path in llm.run_analysis. The fixture at data/fixture_results.json shipped in Phase 3 and provides a valid AnalysisResult. Do NOT depend on Docker or a live Postgres — every DB function is monkeypatched.

    Do NOT add a conftest.py — none exists in this repo and the existing tests run without one (verified by inspecting tests/ contents).
  </action>
  <acceptance_criteria>
    - `tests/test_api.py` exists and contains exact line `from fastapi.testclient import TestClient`
    - `tests/test_api.py` contains `import pytest`
    - `tests/test_api.py` contains `from unittest.mock import MagicMock`
    - `tests/test_api.py` defines at least 14 test functions (one per requirement in <action>) — `grep -c "^def test_" tests/test_api.py` returns >= 14
    - `tests/test_api.py` contains all 14 specific test names: test_health_returns_status_version_service, test_health_no_auth_required, test_analyze_missing_api_key_returns_401, test_analyze_wrong_api_key_returns_401, test_analyze_empty_arrays_returns_422, test_analyze_duplicate_crm_session_id_returns_422, test_analyze_no_session_overlap_returns_422, test_analyze_happy_path_returns_200_with_analysis_result, test_analyze_persists_one_row_per_campaign_with_shared_run_id, test_webhook_missing_api_key_returns_401, test_webhook_happy_path_returns_202_and_calls_insert_pending, test_campaigns_actions_missing_api_key_returns_401, test_campaigns_actions_returns_404_when_no_results, test_campaigns_actions_returns_latest_result_dict
    - `tests/test_api.py` contains the literal string `monkeypatch.setenv("DEMO_MODE", "1")` (DEMO_MODE fixture path forced in happy-path tests)
    - `tests/test_api.py` contains the literal string `monkeypatch.setattr("llm.client", None)` (fixture path requires client is None)
    - `tests/test_api.py` contains the literal string `monkeypatch.setattr("api.main.init_db"` (lifespan init_db patched to avoid real DB)
    - `tests/test_api.py` contains the literal string `monkeypatch.setattr("api.main.insert_analysis_result"` (DB side-effect patching uses api.main namespace, not api.db, because api/main.py imports the name into its local namespace)
    - `tests/test_api.py` contains the literal string `monkeypatch.setattr("api.main.insert_pending_session"`
    - `tests/test_api.py` contains the literal string `monkeypatch.setattr("api.main.get_latest_result"`
    - `tests/test_api.py` does NOT contain the literal string `requests.post` or `requests.get` (use TestClient, not the requests library)
    - `tests/test_api.py` does NOT contain `OPENAI_API_KEY` (must run without live key — DEMO_MODE fixture path only)
    - `uv run pytest tests/test_api.py -v` exits 0 and reports >= 14 tests passing
    - `uv run pytest tests/ -v` (all tests including pre-existing) exits 0 — no regressions in existing tests (test_data, test_llm, test_ui_helpers, test_deploy_config, test_llm_eval)
    - `uv run pytest tests/test_api.py::test_analyze_persists_one_row_per_campaign_with_shared_run_id -v` exits 0 — the specific persistence-with-shared-run_id assertion passes (API-02 verification)
    - `uv run pytest tests/test_api.py::test_health_no_auth_required -v` exits 0 — the negative auth test passes (API-05 explicit no-auth verification)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_api.py -v --tb=short && uv run pytest tests/ -v --tb=short -x</automated>
  </verify>
  <done>tests/test_api.py exposes >=14 test functions covering all 5 endpoints + auth gates + persistence side effects; all tests pass with `uv run pytest tests/test_api.py -v`; pre-existing tests still pass; no live OpenAI or Postgres required (DEMO_MODE fixture + monkeypatched db functions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → api.main app | Tests instantiate the FastAPI app in-process via TestClient; no network exposure; no real DB; no real LLM |
| Test process → llm.py fixture | Tests use DEMO_MODE=1 + llm.client=None to read data/fixture_results.json from disk — no OpenAI API call, no API key in test env |
| Test fixtures → repo filesystem | Tests read data/fixture_results.json which was committed in Phase 3 — bounded by file existence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure (test logs leaking real API key) | tests/test_api.py env handling | mitigate | Tests use `monkeypatch.setenv("API_KEY", "test-key")` — `"test-key"` is a literal sentinel value, never a real secret. Acceptance gate asserts the literal `"test-key"` pattern is present and that no `OPENAI_API_KEY` reference appears in the test file. |
| T-05-04-02 | Spoofing (test passes when auth is broken because env var is leaked from CI shell) | tests/test_api.py auth setup | mitigate | Every test that asserts 401-on-no-header sets `API_KEY=test-key` via monkeypatch.setenv (which auto-rolls back). Tests that assert 401 do NOT send the header — they rely on the missing-header branch of verify_api_key. Without explicitly setting API_KEY, a CI environment with no API_KEY env var would still make verify_api_key return 401 (the `not expected` branch), so the 401 assertions are robust. |
| T-05-04-03 | Tampering (monkeypatch leaks across tests, hiding real bugs) | tests/test_api.py monkeypatch usage | mitigate | pytest's `monkeypatch` fixture auto-rolls back after each test. All env vars, llm.client overrides, and api.main.* function overrides revert at test boundary. No global state mutation outside monkeypatch. |
| T-05-04-04 | Repudiation (test relies on real DB and silently mock-passes if DB unreachable) | tests/test_api.py DB monkeypatching | mitigate | Tests monkeypatch the 4 db functions imported into api.main namespace. With patches in place, no real connect is attempted. If a future test forgets to patch, it will fail with KeyError on DATABASE_URL (loud failure), not silent pass. |
| T-05-04-05 | Information Disclosure (test fixture file contains real customer data) | data/fixture_results.json (Phase 3 artifact) | accept | The fixture was created in Phase 3 from synthetic demo data only (verified by Phase 3 SUMMARY + PROJECT.md constraint "Synthetic/mock data only"). No real customer data. |
| T-05-04-06 | Spoofing (test passes against a wrong-behavior implementation because assertions are too loose) | tests/test_api.py assertion strength | mitigate | API-05 test asserts the EXACT response dict (not a subset/superset); API-02 persistence test asserts BOTH call_count AND that all run_id values are equal AND that run_id parses as UUID AND that the set of campaign_id args matches the response set — four independent assertions on the same side effect. Auth tests assert the EXACT status_code 401 (not "not 200"). |
</threat_model>

<verification>
1. `tests/test_api.py` file exists and is at least 100 lines
2. `grep -c "^def test_" tests/test_api.py` returns >= 14
3. All 14 named tests are present (literal-string acceptance gates)
4. `uv run pytest tests/test_api.py -v` exits 0 with 14+ passing tests
5. `uv run pytest tests/ -v` exits 0 — no regression in pre-existing tests
6. The specific persistence test passes: `uv run pytest tests/test_api.py::test_analyze_persists_one_row_per_campaign_with_shared_run_id -v` exits 0
7. The specific no-auth health test passes: `uv run pytest tests/test_api.py::test_health_no_auth_required -v` exits 0
8. No live-service dependencies: test file does not import `requests`, does not reference `OPENAI_API_KEY`, does not connect to a real Postgres
</verification>

<success_criteria>
- All 5 phase requirements are verified by automated tests:
  - API-01: tests 3+4 (auth) + 5+6+7+8 (validation + happy path)
  - API-02: test 9 (one row per campaign with shared UUID run_id)
  - API-03: tests 10 (auth) + 11 (202 + insert_pending_session called)
  - API-04: tests 12 (auth) + 13 (404) + 14 (200 + returns dict)
  - API-05: tests 1 (exact response) + 2 (no auth required)
- `uv run pytest tests/test_api.py -v` exits 0
- `uv run pytest tests/ -v` exits 0 (no regression)
- Tests run without live OpenAI key (DEMO_MODE fixture path) and without live Postgres (monkeypatched db functions)
- Pattern S6 from PATTERNS.md is honored: DEMO_MODE=1 + monkeypatched llm.client + TestClient pattern
</success_criteria>

<output>
After completion, create `.planning/phases/05-fastapi-service/05-04-SUMMARY.md` documenting:
- Final tests/test_api.py line count and test function count
- pytest output summary (passing test count, runtime)
- Mapping of each test function to the requirement(s) it covers (API-01..05)
- Confirmation that grep gates for `requests.post` and `OPENAI_API_KEY` returned 0
- Confirmation that `uv run pytest tests/` (full suite) exits 0
- Any deviations from PATTERNS.md Pattern S6 or RESEARCH.md "httpx TestClient pattern" (should be none)
</output>
