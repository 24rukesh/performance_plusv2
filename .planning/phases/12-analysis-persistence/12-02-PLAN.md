---
phase: 12
plan: "12-02"
title: "llm.py — OpenAI error fallback (DEMO_MODE auto-fallback)"
wave: 1
depends_on: []
files_modified:
  - llm.py
  - tests/test_llm.py
autonomous: true
requirements:
  - MGMT-03

must_haves:
  goal: "run_analysis() falls back to _load_fixture() on any openai.OpenAIError and sets api_fallback_active=True in session state, while ValueError and other non-OpenAI exceptions still propagate unhandled"
  truths:
    - "run_analysis() wraps _call_llm() in try/except openai.OpenAIError — not except Exception"
    - "On openai.OpenAIError: st.session_state['api_fallback_active'] = True is set (guarded with hasattr check), then _load_fixture() is returned"
    - "On ValueError from _call_llm(): exception propagates — is NOT caught by the except openai.OpenAIError clause"
    - "The import streamlit as st is inside the except branch only (not at module level), guarding against non-Streamlit test contexts"
    - "The existing explicit DEMO_MODE path (if os.environ.get('DEMO_MODE') == '1' and client is None) is unchanged"
    - "tests/test_llm.py adds or augments test_fixture_validates_against_current_schema asserting len(result.campaigns) >= 3 — fallback selectivity tests live in test_evals.py only"
    - "_log_analysis_run is called after every run_analysis() resolution via a guarded `try: import st_db; st_db._log_analysis_run(...) except Exception: pass` block"
  artifacts:
    - path: "llm.py"
      provides: "Modified run_analysis() with try/except openai.OpenAIError fallback"
      contains: "except openai.OpenAIError"
    - path: "tests/test_llm.py"
      provides: "Fallback selectivity tests"
  key_links:
    - from: "run_analysis() except branch"
      to: "st.session_state['api_fallback_active']"
      via: "local import streamlit as st + hasattr guard"
      pattern: "hasattr.*session_state"
    - from: "run_analysis() except branch"
      to: "_load_fixture()"
      via: "return after setting session state flag"
      pattern: "return _load_fixture"
---

<objective>
Modify llm.py to wrap the _call_llm() call in run_analysis() with try/except openai.OpenAIError, falling back to _load_fixture() and setting api_fallback_active=True in session state on any OpenAI SDK exception (D-07/D-08/D-09 from CONTEXT.md).

Purpose: Keep the demo fully functional when the API is unavailable without silently swallowing non-OpenAI errors (ValueError from refusal, pydantic.ValidationError, etc.).

Output: Modified llm.py with the fallback block, and two new tests added to tests/test_llm.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-analysis-persistence/12-CONTEXT.md
@.planning/phases/12-analysis-persistence/12-AI-SPEC.md
@.planning/phases/12-analysis-persistence/12-PATTERNS.md

<interfaces>
<!-- Current run_analysis() in llm.py — exact text the executor must modify. -->

Current llm.py run_analysis() (lines 118–131):
```python
def run_analysis(campaign_agg: pd.DataFrame) -> AnalysisResult:
    """Serialize campaign_agg as CSV and send to gpt-4o. Returns a validated AnalysisResult."""
    # per D-01, D-11: fixture path when DEMO_MODE=1 and no live key is present
    if os.environ.get("DEMO_MODE") == "1" and client is None:
        return _load_fixture()
    t0 = time.perf_counter()
    csv_text = campaign_agg.to_csv(index=False)
    result = _call_llm(csv_text)
    logger.info(
        "run_analysis completed in %.2fs | campaigns=%d",
        time.perf_counter() - t0,
        len(result.campaigns),
    )
    return result
```

Target shape after modification (per AI-SPEC §4 Implementation Guidance):
```python
def run_analysis(campaign_agg: pd.DataFrame) -> AnalysisResult:
    """..."""
    if os.environ.get("DEMO_MODE") == "1" and client is None:
        return _load_fixture()
    t0 = time.perf_counter()
    csv_text = campaign_agg.to_csv(index=False)
    try:
        result = _call_llm(csv_text)
    except openai.OpenAIError as exc:
        logger.warning("OpenAI API error — falling back to fixture: %s", exc)
        import streamlit as st
        if hasattr(st, "session_state"):
            st.session_state["api_fallback_active"] = True
        return _load_fixture()
    logger.info(
        "run_analysis completed in %.2fs | campaigns=%d",
        time.perf_counter() - t0,
        len(result.campaigns),
    )
    return result
```

Existing import at top of llm.py: `from openai import OpenAI` — add `import openai` as a second import line from the openai package so openai.OpenAIError is accessible.

Existing _call_llm() tenacity decorator: @retry with retry_if_not_exception_type(ValueError) — this retries on OpenAIError (correct). The try/except in run_analysis() fires only after all 3 tenacity retries are exhausted.

Existing _load_fixture() (lines 134–138): unchanged — already validates via AnalysisResult.model_validate(data).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add try/except openai.OpenAIError to run_analysis()</name>
  <files>llm.py</files>
  <read_first>
    - llm.py — read entire file to see current imports, run_analysis() body, _call_llm() decorator, and _load_fixture()
    - .planning/phases/12-analysis-persistence/12-CONTEXT.md — D-07, D-08, D-09 (exact fallback requirements)
    - .planning/phases/12-analysis-persistence/12-AI-SPEC.md — §3 Common Pitfalls (especially pitfall #1: catch Exception vs OpenAIError, and pitfall #2: api_fallback_active reset)
    - .planning/phases/12-analysis-persistence/12-PATTERNS.md — Modified run_analysis() pattern in llm.py section
  </read_first>
  <behavior>
    - import openai is added to the imports block (alongside the existing from openai import OpenAI line) so openai.OpenAIError base class is accessible
    - run_analysis() DEMO_MODE early-return path is unchanged
    - result = _call_llm(csv_text) is wrapped in try:
    - except openai.OpenAIError as exc: catches the exception, logs a warning with logger.warning, does a local "import streamlit as st", checks hasattr(st, "session_state") before setting st.session_state["api_fallback_active"] = True, then returns _load_fixture()
    - The logger.info call remains in the success path after the try block (not inside try:)
    - No except Exception, no except ValueError — only except openai.OpenAIError
    - _load_fixture() is not modified
    - After the try/except block resolves (both success and fallback paths), add a guarded _log_analysis_run call:
      `try: import st_db as _st_db; _st_db._log_analysis_run(run_mode=_run_mode, error_type=_err_type, campaign_count=len(result.campaigns), latency_ms=int((time.perf_counter()-t0)*1000), api_fallback_active=_fallback_fired); except Exception: pass`
      where `_run_mode` is "fixture" if fallback fired else "live", `_err_type` is `type(exc).__name__` (from the except branch) or None (success path), `_fallback_fired` is True/False — use a local bool variable to track across the try/except.
  </behavior>
  <action>
    Edit llm.py to add `import openai` at the top of the imports block (after `from openai import OpenAI`). Then modify the body of run_analysis() to the following structure:

    1. Keep the existing DEMO_MODE early-return guard unchanged.
    2. After `csv_text = campaign_agg.to_csv(index=False)`, add `_fallback_fired = False` and `_err_type = None`.
    3. Wrap `result = _call_llm(csv_text)` in a `try:` block.
    4. In the `except openai.OpenAIError as exc:` branch: set `_fallback_fired = True` and `_err_type = type(exc).__name__`, call `logger.warning(...)`, do a local `import streamlit as st`, guard `st.session_state["api_fallback_active"] = True` with `hasattr(st, "session_state")`, then assign `result = _load_fixture()` (do NOT return here yet — assign result so the logging call below can use `len(result.campaigns)`).
    5. After the try/except block (in the normal code flow reached by both paths — success path falls through, fallback path assigns result inside except then falls through): add the logging call:
       `try: import st_db as _st_db; _st_db._log_analysis_run(run_mode="fixture" if _fallback_fired else "live", error_type=_err_type, campaign_count=len(result.campaigns), latency_ms=int((time.perf_counter() - t0) * 1000), api_fallback_active=_fallback_fired); except Exception: pass`
    6. After the logging call: add `if _fallback_fired: return result` (so fallback path exits here), then keep the existing `logger.info(...)` and `return result` for the success path.

    The except clause must use `openai.OpenAIError` (via the openai module, not a bare import). Do not add except Exception, except ValueError, or except pydantic.ValidationError.
  </action>
  <verify>
    <automated>cd /Users/rukesh/Documents/Dev/performance_plus && grep -c "except openai.OpenAIError" llm.py && grep -c "_log_analysis_run" llm.py</automated>
  </verify>
  <done>
    llm.py contains exactly one `except openai.OpenAIError as exc:` clause inside run_analysis(). `import openai` is present at the top of the file. `_log_analysis_run` appears at least once in llm.py (the guarded call). The DEMO_MODE guard and _load_fixture() are unchanged. `grep -c "_log_analysis_run" llm.py` returns 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fallback selectivity tests to tests/test_llm.py</name>
  <files>tests/test_llm.py</files>
  <read_first>
    - tests/test_llm.py — read full file to understand existing fixtures (_minimal_agg_df, _make_valid_analysis_result, _make_mock_completion), mock patterns used, and existing test names to avoid collisions
    - llm.py — the modified run_analysis() (from Task 1) — confirm the try/except structure before writing tests against it
    - .planning/phases/12-analysis-persistence/12-AI-SPEC.md — §5 Dimension 1 (fallback trigger selectivity) for the parametrize list and the non-fallback assertion
  </read_first>
  <behavior>
    - test_fixture_validates_against_current_schema (add if not already present): call _load_fixture(), assert isinstance(result, AnalysisResult), assert len(result.campaigns) >= 3 — ensures the fixture stays in sync with the current schema after Phase 12 changes
    - NOTE: test_openai_error_triggers_fallback and test_value_error_does_not_trigger_fallback belong in tests/test_evals.py (Plan 12-04) only — do NOT add them here to avoid test duplication across test_llm.py and test_evals.py
    - All existing tests must continue to pass (do not delete or modify existing tests)
  </behavior>
  <action>
    Append new test to tests/test_llm.py after the last existing test.

    For test_fixture_validates_against_current_schema: call llm._load_fixture() and assert isinstance(result, AnalysisResult) and len(result.campaigns) >= 3. Check if a test_fixture_schema test already exists — if so, augment it with the len >= 3 assertion rather than duplicating the entire function. This test guards against fixture drift when Phase 12 schema changes are made.

    Do NOT add test_openai_error_triggers_fallback or test_value_error_does_not_trigger_fallback here — those canonical fallback selectivity tests live in tests/test_evals.py (Plan 12-04).

    Run full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/rukesh/Documents/Dev/performance_plus && uv run pytest tests/test_llm.py -x -q</automated>
  </verify>
  <done>
    tests/test_llm.py contains test_fixture_validates_against_current_schema asserting isinstance(result, AnalysisResult) and len(result.campaigns) >= 3. All existing tests still pass. uv run pytest tests/test_llm.py -x -q exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenAI API → llm.py | The SDK raises exception types from openai.OpenAIError hierarchy on API failure. The except clause must catch only those types, not broader Python exceptions. |
| llm.py → st.session_state | The except branch writes api_fallback_active=True to Streamlit session state. This must be guarded against non-Streamlit contexts (tests, CLI usage). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | except openai.OpenAIError scope | mitigate | Catch only openai.OpenAIError base class — not Exception. Prevents masking of pydantic.ValidationError from a broken fixture, which would allow silently wrong data to appear as a successful fallback. Enforced by the parametrized test_value_error_does_not_trigger_fallback test. |
| T-12-06 | Information Disclosure | logger.warning output | accept | Exception type and message are logged to stderr/loguru. No credentials or user data appear in the exception message from OpenAI SDK exceptions. Acceptable for self-hosted VPS with local log access. |
| T-12-07 | Spoofing | Fixture output displayed as live | mitigate | app.py renders st.warning("OpenAI API unavailable — showing cached demo results") when api_fallback_active=True. Implemented in Plan 12-03. Fixture campaign IDs use synthetic names (DEMO_CAMPAIGN_*) to make domain mismatch visible. |
| T-12-SC | Tampering | pip/cargo installs | mitigate | No new packages installed — openai is already a dependency. Slopcheck not triggered. |
</threat_model>

<verification>
import openai appears in llm.py imports: grep -n "^import openai" llm.py
except openai.OpenAIError present: grep -c "except openai.OpenAIError" llm.py returns 1
hasattr guard present: grep -c "hasattr(st" llm.py returns >= 1
Fallback tests pass: uv run pytest tests/test_llm.py -x -q exits 0
Full suite passes: uv run pytest tests/ -x -q exits 0
</verification>

<success_criteria>
llm.py wraps _call_llm() in try/except openai.OpenAIError. On any OpenAI SDK exception: logger.warning fires, api_fallback_active=True is set in session state (guarded by hasattr), and _load_fixture() is returned. ValueError still propagates. All existing test_llm.py tests pass. test_openai_error_triggers_fallback covers RateLimitError, AuthenticationError, APIConnectionError. test_value_error_does_not_trigger_fallback asserts ValueError is not swallowed.
</success_criteria>

<output>
Create .planning/phases/12-analysis-persistence/12-02-SUMMARY.md when done
</output>
